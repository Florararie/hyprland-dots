general {
    hide_cursor = false
}


animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}


# background image
background {
  monitor = DP-2
  #path = ~/.config/hypr/images/lock.jpg
  path = screenshot # if you want your desktop background
  color = rgba(20, 18, 24, 1.0)

  blur_passes = 3
  noise = 0.10
  brightness = 0.65
  vibrancy = 0.25
  vibrancy_darkness = 0.25
}

background {
  monitor = HDMI-A-1
  path = screenshot
  color = rgba(20, 18, 24, 1.0)

  blur_passes = 3
  noise = 0.10
  brightness = 0.65
  vibrancy = 0.25
  vibrancy_darkness = 0.25
}


# time
label {
  monitor = DP-2 
  text = cmd[update:1000] echo "$(date +%I:%M:%S\ %p)"
  text_align = center
  color = rgba(230, 230, 230, 1.0)
  font_size = 120
  font_family = JetBrains Mono Bold
  shadow_passes = 1
  shadow_size = 3
  shadow_color = rgba(0, 0, 0, 0.5)

  position = 0, 320
  halign = center
  valign = center
}


# date
label {
  monitor = DP-2
  text = cmd[update:60000] echo "$(date +"%A, %B %d")"
  text_align = center
  color = rgba(190, 190, 190, 0.9)
  font_size = 38
  font_family = JetBrains Mono Medium
  shadow_passes = 1
  shadow_size = 3
  shadow_color = rgba(0, 0, 0, 0.5)

  position = 0, 200
  halign = center
  valign = center
}

# shutdown
label {
  monitor = DP-2
  text =  Shutdown
  font_family = JetBrains Mono Light
  font_size = 22
  color = rgba(255, 255, 255, 0.9)
  position = -280, 80
  halign = center
  valign = bottom
  shadow_passes = 1
  shadow_size = 3
  shadow_color = rgba(0, 0, 0, 0.5)
  onclick = systemctl poweroff
}

# reboot
label {
  monitor = DP-2
  text =  Reboot
  font_family = JetBrains Mono Light
  font_size = 22
  color = rgba(255, 255, 255, 0.9)
  position = -80, 80
  halign = center
  valign = bottom
  shadow_passes = 1
  shadow_size = 3
  shadow_color = rgba(0, 0, 0, 0.5)
  onclick = systemctl reboot
}

# suspend
label {
  monitor = DP-2
  text = 󰤄 Suspend
  font_family = JetBrains Mono Light
  font_size = 22
  color = rgba(255, 255, 255, 0.9)
  position = 110, 80
  halign = center
  valign = bottom
  shadow_passes = 1
  shadow_size = 3
  shadow_color = rgba(0, 0, 0, 0.5)
  onclick = systemctl suspend
}

# logout
label {
  monitor = DP-2
  text =  Logout
  font_family = JetBrains Mono Light
  font_size = 22
  color = rgba(255, 255, 255, 0.9)
  position = 300, 80
  halign = center
  valign = bottom
  shadow_passes = 1
  shadow_size = 3
  shadow_color = rgba(0, 0, 0, 0.5)
  onclick = hyprctl dispatch exit
}

input-field {
  monitor = DP-2
  size = 400, 60
  outline_thickness = 2
  dots_size = 0.25
  dots_spacing = 0.35
  dots_center = true

  outer_color = rgba(180, 180, 180, 0.4)
  inner_color = rgba(20, 20, 20, 0.5)
  font_color = rgb(230, 230, 230)

  fade_on_empty = true
  rounding = 10
  check_color = rgb(137, 180, 250)
  fail_color = rgb(255, 100, 100)

  placeholder_text = <i><span foreground="##cdd6f4">Enter Password...</span></i>
  fail_text =
  hide_input = false

  position = 0, -40
  halign = center
  valign = center

  shadow_passes = 2
  shadow_size = 6
  shadow_color = rgba(0, 0, 0, 0.7)
}